"use client";
// import React from "react";

const Searchbar = () => {
  const handleSearch = () => {};
  return (
    <form className="searchbar" onSubmit={handleSearch}>
      <div></div>
    </form>
  );
};

export default Searchbar;
